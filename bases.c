#include <stdio.h>
//显示二进制
int main(void)
{
    int x;
    // printf("desc = %d,octal = %o; hex = %x\n",x,x,x);
    // printf("desc = %d,octal = %#o; hex = %#x\n",x,x,x);
    printf("Please Input A ascII:");
    scanf("%2d",x);
    return 0;
}